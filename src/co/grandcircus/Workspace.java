package co.grandcircus;

public class Workspace {

	/*
	 * Player p = new RockPlayer(); 
	 * Player (abstract) 
	 * RockPlayer (subclass of player)
	 * RandomPlayer (subclass of Player)
	 * HumanPlayer (can be sub of player too)
	 * RoshamboApp (main method)
	 * RoshamboEnum (optional but encouraged, see section 4.11 in zybooks)
	 */

}
